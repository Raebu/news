# Admin application

The admin application is intentionally scaffold-only in this pass. It will consume authenticated platform APIs for editorial review, exception handling, evidence inspection, policy/config management and operational metrics. No production UI is claimed yet.
